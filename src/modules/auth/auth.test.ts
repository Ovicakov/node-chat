import { describe, it, expect, vi, beforeAll } from "vitest";

import { app } from "@src/app.ts";

describe("Auth", () => {
  it("should return jwt et refresh token does not have it", () => {
    expect(true).toBe(true);
  });
});
